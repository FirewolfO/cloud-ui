# Cloud UI 开发约束

## 仓库定位

- 本仓库是整个云服务的统一 Web 入口，负责登录、注册、账号入口、产品导航和后续云资源控制台页面；不在前端实现后端业务规则或持久化服务数据。
- 登录与注册由独立的 `sigin` 后端提供，资源权限由 `permission` 系统判定，运行时业务请求按系统规划接入 Gateway。前端只能呈现权限结果，不能作为最终授权边界。
- 本仓库与 `sigin`、Gateway、Permission 等后端保持前后端分离，所有集成都通过版本化 API 契约完成。

## 技术选型

- 使用 Vue 3、TypeScript 和 Vite，组件统一采用 Composition API 与 `<script setup lang="ts">`。
- 使用 Vue Router 管理页面和认证路由，使用 Pinia 管理用户会话、全局产品状态和确有跨页面需求的状态，局部状态保留在组件或 composable 中。
- UI 组件库统一使用 Element Plus，图标使用 `@element-plus/icons-vue`。不得在同一应用中再引入第二套完整组件库。
- 包管理统一使用 npm，并提交 `package-lock.json`；Node.js 版本通过项目约定文件固定。依赖升级必须连同类型检查和生产构建一起验证。
- 选择这套技术栈是为了与现有 `gateway-ui` 保持一致，并满足云控制台对表单、表格、导航、权限状态和长期模块化扩展的需求。未经明确架构评审，不切换到 React、Angular、Nuxt 或其他应用框架。

## 架构与接口

- 页面按业务域拆分，通用布局、基础组件、API Client、认证状态、类型和工具函数分别集中管理；禁止把请求、状态和复杂业务判断全部堆在页面组件中。
- 所有 HTTP 请求必须通过统一 API Client 发起，集中处理基础地址、请求 ID、超时、错误转换和认证失效；组件中不得散落硬编码服务地址。
- API 地址通过 `VITE_` 环境变量配置，仓库只提交无敏感信息的示例文件。OpenAPI 契约稳定后优先生成 TypeScript 类型，禁止长期手写与后端不一致的重复模型。
- 路由守卫负责登录态跳转和页面进入条件，但真实权限必须由后端校验。未登录、无权限、会话过期、接口失败、空数据和加载中状态都必须有完整页面行为。
- 公共组件只在出现真实复用后提取；产品域之间通过清晰接口协作，不直接修改其他域内部状态。

## 登录与浏览器安全

- 登录密码只保存在当前表单内，提交结束后及时清理；不得写入日志、URL、Pinia 持久化、`localStorage`、`sessionStorage` 或埋点。
- 优先通过后端设置的 `HttpOnly`、`Secure`、合理 `SameSite` Cookie 维持浏览器会话，前端请求按需启用 credentials，并配合后端 CSRF 机制。不得为了方便改成可被 JavaScript 读取的长期 Token。
- 用户信息和会话状态集中由认证 store 管理。退出登录必须调用后端使会话失效并清理所有内存状态；只删除前端展示状态不算完成退出。
- 登录、注册、找回密码页面不得泄露账号是否存在等额外信息，不得绕过后端验证码、频率限制或风控结果。
- 页面渲染不信任服务端返回的 HTML；确需渲染富文本时必须使用经过评审的消毒方案，禁止随意使用 `v-html`。

## 交互与视觉

- 云控制台应安静、清晰、适合长时间操作，优先信息层级、扫描效率和稳定导航；不要做成营销落地页，也不要使用装饰性大卡片、夸张圆角或大面积单色渐变。
- 桌面端提供稳定的全局导航和产品切换入口，移动端必须保证登录注册与关键查看流程可用。固定格式控件需要稳定尺寸，文本不得溢出或相互遮挡。
- 使用熟悉图标表达常规工具操作并提供 tooltip；表单、按钮、菜单、分页、筛选和确认流程遵循 Element Plus 交互规范。
- 所有表单必须有客户端基础校验和服务端错误反馈，但客户端校验不能替代后端校验。提交期间避免重复操作，危险操作需要明确确认。
- 满足基础无障碍要求：语义化结构、可见焦点、键盘操作、表单标签和足够的颜色对比度。

## 质量与工作流

- 新功能至少覆盖关键 composable、store 和交互逻辑测试；登录、注册、会话恢复、退出和路由守卫属于必须验证的核心流程。
- 提交前必须运行项目已有的 lint、类型检查、单元测试和生产构建。新增页面需检查桌面与移动视口，确保无布局溢出、遮挡或空白主界面。
- 本仓库是独立 Git 仓库。每次修改完成并通过必要验证后，必须直接创建提交并推送当前分支；提交中不得混入其他仓库或无关文件。
